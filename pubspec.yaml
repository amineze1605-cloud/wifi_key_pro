name: wifi_key_pro
description: WiFi password generator premium
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  qr_flutter: ^4.1.0
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true
